export enum Lang {
  ru = "ru",
  en = "en",
}

export enum LogType {
  info = "info",
  warning = "warning",
  success = "success",
  error = "error",
}

export enum Mode {
  prod = "prod",
  dev = "dev",
}
